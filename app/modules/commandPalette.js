// constants
// const SHOW_COMMAND_PALETTE = 'git-viewer/SHOW_COMMAND_PALETTE';
// const HIDE_COMMAND_PALETTE = 'git-viewer/HIDE_COMMAND_PALETTE';

// initial state
const initialState = {
  command: '',
  visible: false
};

// reducer
export default function reducer (state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

// action creators
